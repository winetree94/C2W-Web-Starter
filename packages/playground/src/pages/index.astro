---
import C2WTerminal from "../components/C2WTerminal.astro";
import chunks from "../components/chunks.json";
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <section>
    <C2WTerminal imageName={"alpine"} />
  </section>
  <hr>
  <section>
    <h1>x86_64 containers demo</h1>
    <table>
      <thead>
        <tr>
          <th>Name</th>
          <th>Image</th>
          <th>Size</th>
          <th>Demo</th>
        </tr>
      </thead>
      <tbody>
        {
          Object.values(chunks).map((image) => (
            <tr>
              <td>{image.name}</td>
              <td>
                <Fragment set:html={image.image.replaceAll("\n", "<br>")} />
              </td>
              <td>{Math.ceil(image.size / 1024 / 1024)}MB</td>
              <td>
                <a href={`/playground/${image.name}/`}>Run</a>
              </td>
            </tr>
          ))
        }
      </tbody>
    </table>
  </section>
</Layout>
